main() {
  const hour = DataTime.now().hour;
  print(hour);
}

//const is a variable type that can only be used with constant values 
//like the (pi π) for example or any other values that we don't want it to be changed
